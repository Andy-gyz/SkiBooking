# Architecture

The planned system architecture and implementation constraints are defined in
[`PROJECT_CONTEXT.md`](../PROJECT_CONTEXT.md). This document will record the
implemented architecture as the project evolves.

